<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>hello</title>
    <style>

        ul,h2{
            margin: 0;
            /* padding: 0; */
        }
        li{
            list-style: none;
        }
    </style>
</head>
<body>
    <% for( let item in result){ %>
        <h2><%=item%></h2>
        <ul>
            <% result[item].forEach((val)=>{   %>

                <li><a href="<%= val['url']%>" target="_block"><%=val['title']%></a></li>

            <% }) %>
        </ul>
        
    <%} %>
</body>
</html>